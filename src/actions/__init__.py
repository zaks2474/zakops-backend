"""ZakOps Actions (Kinetic Action Engine).

This package provides:
- Capability manifests + registry (schema-driven UI + planning)
- Kinetic action state store + runner
- Executor plugins that generate artifacts into the DataRoom
"""

